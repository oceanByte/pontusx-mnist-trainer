# Pontus-X MNIST Trainer

PyTorch training script (`train_mnist.py`) that loads the HuggingFace `ylecun/mnist` parquet dataset and trains a small CNN. Includes CLI flags for epochs/batch size/output directory and saves the trained weights.
